server:
  domain: "http://localhost:8080"
  port: ":3000"
database:
  host: localhost
  port: 5432
  name: postgres
  user: postgres
  password: Abittu@2000
  connect_timeout: 10
  sslmode: prefer
  migrate: true
  seed: true
